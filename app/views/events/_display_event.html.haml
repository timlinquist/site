%li.event-box
  .event-box-outer
    .event-box-inner
      =link_to image_tag(display_event.logo.url(:small)), event_path(display_event), :class => 'noborder'
      %br
      %span.small
        =link_to display_event.date_occurred, event_path(display_event), :class => 'noborder'
